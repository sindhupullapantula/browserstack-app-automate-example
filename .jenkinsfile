pipeline {
agent any
stages {
    stage('test') {
        steps {
                sh 'npm install'
                sh 'npm run test-local'
                sh 'npm run test-bs'
                sh 'npm run test-local-ios'
                sh 'npm run test-bs-ios'
            }
        }
    }
}
